# Third-party licenses

This binary is built from Rust crates under the licenses listed below. It also embeds
timezone boundary polygons derived from OpenStreetMap data, carried by the `tzf-dist`
crate, which are licensed under the Open Database License (ODbL) v1.0 — the full text
is not shipped; it is at https://opendatacommons.org/licenses/odbl/1-0/ .

Generated by cargo-about 0.9.1; committed inputs are `about.toml` and `about.hbs`.
Regenerate from the repo root, never hand-edit:

    cargo about generate about.hbs -o THIRD-PARTY-LICENSES

## Overview

{{#each overview}}
- {{{name}}} ({{count}})
{{/each}}

## License texts

{{#each licenses}}
### {{{name}}} ({{id}})

Used by:

{{#each used_by}}
- {{{crate.name}}} {{crate.version}}
{{/each}}

{{#if (eq id "ODbL-1.0")}}
The full ODbL-1.0 text is not reproduced here; it is at
https://opendatacommons.org/licenses/odbl/1-0/ .
{{else}}
```text
{{{text}}}
```
{{/if}}

{{/each}}
